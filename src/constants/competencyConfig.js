/**
 * 역량 평가 설정
 * - use4060: false → KPI 정의서 우선 (팀장 확정만 level 반영)
 * - use4060: true → 설계노트 40:60 (자체×0.4 + 팀장×0.6)
 */
export const COMPETENCY_USE_4060 = false;

export const COMPETENCY_CAP_DEFAULT = 0.4;
export const COMPETENCY_CAP_RELEASE = 10;
