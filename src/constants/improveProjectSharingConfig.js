// Vercel Blob is currently suspended, so keep Blob-backed sharing controls out of production UI.
export const SHOW_BLOB_IMPROVE_PROJECT_SHARING_UI = false;

// B/C member journals should use the leader-provided JSON flow while Blob-backed sharing is disabled.
export const SHOW_BC_JOURNAL_TEAM_SHARE_UI = false;
